# world parameters
world.width=100
world.height=100
world.map=base.map
world.fire=base.fire

# agent parameters
agent.num.scout=10
agent.num.worker=10
agent.deployment=Agent.DeploymentStrategy.Random
agent.sensor=Agent.Sensor.Circle
agent.plan=Agent.Plan.None
agent.comm=Agent.Comm.None
agent.propulsion=Agent.Propulsion.Thruster

# sim params (if any)
